{
  "version": 2,
  "buildCommand": "npx prisma generate && pnpm run build",
  "devCommand": "pnpm run dev",
  "outputDirectory": ".next",
  "functions": {
    "app/api/**/*": {
      "maxDuration": 60
    }
  }
}